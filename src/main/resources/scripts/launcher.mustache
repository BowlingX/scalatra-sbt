#!/bin/bash
set -e
# {{name}}, Copyright (c) {{organization}}
# Launcher Script
# VERSION: {{version}}
# Generated by scalatra-sbt-dist-plugin
# Warning: Do not touch this file, it has been Auto Generated

# Setup Java Options
JAVA_OPTIONS=({{#javaOptions}}{{this}} {{/javaOptions}})
# Directory where we are currently in
CURRENT_DIR=$(cd -P -- "$(dirname -- "$0")" && pwd -P)
# Libraries are in lib directory
LIB_DIR="${CURRENT_DIR}/../lib"
# App directory (add's webapp directory to classpath)
APP_DIR="${CURRENT_DIR}/../"

SCALATRA_ENV="production"
JAVA_OPTIONS=("${JAVA_OPTIONS[@]}" "-Dorg.scalatra.environment=$SCALATRA_ENV")

# Execute Launcher class, use exec to use same pid as script
exec java "${JAVA_OPTIONS[@]}" -cp "${APP_DIR}":"${LIB_DIR}":{{classpath}} {{mainClass}} "$@"